# deps / build
node_modules/
dist/

# runtime state — tenant dirs contain gateway tokens and provider secrets
tenants/
data/

# editor
.idea/
.vscode/

*.log

# shared read-only browser cache (large, host-local)
shared/
